apiVersion: v1
kind: Service
metadata:
  name: clippyfunc
  labels:
    name: clippyfunc
spec:
  type: "{{.Values.service.type}}"
  ports:
  - name: http
    port: {{.Values.service.port}}
    targetPort: {{.Values.service.port}}
  selector:
    app: clippyfunc